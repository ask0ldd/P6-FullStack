export interface IUser {
    id: number
    email: string
    lastName: string
    firstName: string
    userName: string
    password: string
    admin: boolean
    createdAt: string
    updatedAt: string
  }